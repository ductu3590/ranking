'use strict';
function validateSetup(draft = {}) { const blockers = []; const warnings = []; if (!String(draft.tournament && draft.tournament.name || '').trim()) blockers.push('TOURNAMENT_NAME_REQUIRED'); const selected = draft.participants && draft.participants.selectedMemberIds || []; if (!selected.length && !(draft.pairs || []).length) blockers.push('ROSTER_EMPTY'); if ((draft.unpairedMemberIds || []).length) blockers.push('UNPAIRED_MEMBER'); if ((draft.pairs || []).some((pair) => !Array.isArray(pair.memberIds) || pair.memberIds.length !== 2)) blockers.push('PAIR_MEMBER_COUNT_INVALID'); if ((draft.participants && draft.participants.inactiveSelectedMemberIds || []).length) warnings.push('INACTIVE_MEMBER_SELECTED'); return { blockers, warnings, ready: blockers.length === 0 }; }
function validateFinalize(draft = {}) { if (draft.matchState && (draft.matchState.started || draft.matchState.hasScore)) return { ok: false, error: { code: 'STRUCTURE_LOCKED_BY_RESULTS', message: 'Không thể đổi cấu trúc sau khi có kết quả.' } }; const result = validateSetup(draft); return result.blockers.length ? { ok: false, error: { code: result.blockers[0], message: result.blockers.join(', ') } } : { ok: true, blockers: [], warnings: result.warnings }; }
module.exports = { validateSetup, validateFinalize };
