Phase 1 Vercel Preview smoke evidence
Date: 2026-09-03 (Asia/Bangkok)
Deployment: https://ranking-aeaclq5rx-do-duc-tus-projects.vercel.app
Project: do-duc-tus-projects/ranking
Source: local worktree codex/phase-1-foundation-hardening
Production was not promoted.
Production alias verification: `https://rank.soccernow.net` still points to the
existing production deployment created 2026-09-02; Preview has no production alias.

Automated checks (Vercel CLI `vercel curl`, deployment protection bypass generated by CLI):
- GET / -> HTTP 200, X-Matched-Path: /
- GET /giai-dau/v2 -> HTTP 200, X-Matched-Path: /giai-dau/v2
- GET /giai-dau/v2/test-5-wsnn -> HTTP 200, X-Matched-Path: /giai-dau/v2/[slug]
- GET /api/tournament-v2/public?slug=test-5-wsnn -> HTTP 200, JSON public snapshot returned
- GET /api/tournament-v2/public (missing slug) -> HTTP 400 Bad Request (expected validation)
- GET /api/tournament-v2/tournaments (anonymous) -> HTTP 401 Unauthorized (expected auth boundary)
- Public snapshot shape: tournament id 7, requested slug echoed, 1 stage, 2 entrants, 1 match, 1 standings stage.

Browser check (Chrome user session):
- Preview home rendered Pickhub UI with “Tạo nhóm mới” and “Tham gia nhóm”.
- /giai-dau/v2 rendered the tournament page and correctly showed “Unauthorized” for guest access.
- Public route loaded the client shell; its API fetch remained in loading state in the browser session while the same endpoint returned HTTP 200 via Vercel CLI. This is a follow-up check for deployment-protection cookie propagation, not a database failure.

Not executed against production data:
- login with a test account, admin/member authorization writes, create tournament/stage/schedule, record result, webhook retry.
These require a dedicated test account and disposable tournament (or a separate staging project) to avoid mutating live production records.
