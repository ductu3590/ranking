# T3.3 Browser Screenshot Prerequisites

Date: 2026-09-20
Task: T3.3

## Required screenshots

The T3.3 plan requires screenshots of the unified setup at 390px mobile, tablet, and desktop. No screenshot is included here because no browser automation executable was available; fabricating image evidence is not acceptable.

## Checks performed

1. Probed `http://localhost:3000/giai-dau/v2`: HTTP 404, so this listener is not the Pickhub setup app.
2. Probed `http://localhost:3001/giai-dau/v2`: HTTP 307 redirect, but no QA session or browser was available to follow and validate the authenticated workspace.
3. Ran Playwright Chromium launch: failed because `C:\Users\ductu\AppData\Local\ms-playwright\chromium_headless_shell-1187\chrome-win\headless_shell.exe` does not exist.
4. Checked QA environment: `PICKHUB_QA_BASE_URL`, `PICKHUB_QA_ADMIN_SESSION`, `PICKHUB_QA_GROUP_ID`, and `CHROME_EXECUTABLE` were unset. No Chrome or Edge executable was on PATH.

## Impact

- Mobile 390px actual horizontal-overflow, touch target, visual focus, and Tab-order checks are blocked.
- Tablet and desktop summary-rail screenshots are blocked.
- Authenticated interaction checks for Enter/Space and per-action loading/error transitions are blocked.

## Unblock

Provide a scoped QA base URL, admin session cookie, group ID, and a Chromium/Chrome executable (or install the Playwright browser). Then rerun `tests/unified-setup-v2/browser/setup.browser.test.js` plus the T3.3 browser capture script to produce the three required PNGs.