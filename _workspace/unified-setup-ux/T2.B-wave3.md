# T2.B Wave 3 Handoff

## Scope

`PICKHUB_TASK_ID=T2.B` was set for all edits and validation. Concurrent work in `lib/tournament/stagePlan.js` and T3 artifacts was left untouched.

## Files changed

- `app/giai-dau/v2/setup/steps/FormatPairingStep.js`
- `app/giai-dau/v2/setup/pairing/PairingBoard.js`
- `app/giai-dau/v2/setup/participants/participants.css`
- `app/giai-dau/v2/setup/pairing/pairing.css`
- `tests/unified-setup-v2/ui/participants-t2b-contract.test.js`

## Delivered behavior

- The odd-member `add_member` remedy adds an available roster member through the pairing-draft add action and synchronizes that member into `participants.selectedMemberIds`.
- The `reserve_member` remedy removes the odd member from the competition selection and records their ID in `pairingDraft.reserveMemberIds` / draft `reserveMemberIds`.
- The `switch_format` remedy updates the draft entrant type to `singles` only when `format.supportedEntrantTypes` explicitly includes it. Otherwise the control is disabled and explains in Vietnamese that singles is not supported by this tournament configuration.
- Participants and pairing controls now have a 44px minimum target and a visible keyboard focus outline.

## Validation

- PASS: `node tests/unified-setup-v2/ui/participants-t2b-contract.test.js`
- PASS: `git diff --check -- app/giai-dau/v2/setup/steps/FormatPairingStep.js app/giai-dau/v2/setup/pairing/PairingBoard.js app/giai-dau/v2/setup/participants/participants.css app/giai-dau/v2/setup/pairing/pairing.css tests/unified-setup-v2/ui/participants-t2b-contract.test.js`
- FAIL (pre-existing / outside T2.B ownership): `node tests/unified-setup-v2/t3.2-edge-journeys.test.js` cannot find `ADVANCE_RESULTS_INCOMPLETE` in `app/api/tournament-v2/advance/route.js`.
- FAIL (pre-existing / outside T2.B ownership): `node tests/unified-setup-v2/t3.3-accessibility-responsive.test.js` reports RWD-01 in `setup.css`, LOAD-02 in draw/review steps, and ERR-01 in `InfoParticipantsStep.js` invite handling. T2.B focus-visible and 44px checks pass.

## Commit

No commit created, as requested.

## Follow-up

- Integrator/API owner should restore the stable advance error code expected by T3.2.
- T2.A/T2.C and T2.E owners should address the remaining T3.3 static QA findings in their respective paths.
