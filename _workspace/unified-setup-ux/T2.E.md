# T2.E - Giao huu lien CLB tren workspace setup

## Scope

- `app/giai-dau/v2/setup/SetupContext.js`
- `app/giai-dau/v2/setup/setup.css`
- `app/giai-dau/v2/setup/steps/InfoParticipantsStep.js`
- `app/giai-dau/v2/TournamentWizard.js`
- `tests/phase3/interclub-ui.test.js`
- `_workspace/unified-setup-ux/ADR-002-friendly-invited-clubs.md`

## Contract

- Workspace stores friendly invitations in `draft.invitedClubs` using the T1.B shape.
- `InfoParticipantsStep` owns the real client calls for `inviteTournamentClub` and related club lookups; `TournamentWizard` does not import unused invite helpers.
- The interclub contract test recursively reads `app/giai-dau/v2/setup/**/*.js` and retains assertions for `Giao hữu`, `inviteTournamentClub`, and `CLB được mời`.

## Red-green evidence

1. Temporarily renamed `inviteTournamentClub` to `sendClubInviteBroken` in `InfoParticipantsStep.js`.
   - `node tests/phase3/interclub-ui.test.js`
   - Result: red, `FAIL: wizard mời CLB khác`, exit `1`.
2. Restored `inviteTournamentClub`.
   - `node tests/phase3/interclub-ui.test.js`
   - Result: green, `phase3 interclub ui contract ok`, exit `0`.

## Exit gate

First full run found `wizard-redesign-contract.test.js` red on static `select-visible`; second run then found static `Xóa`; third run found static `STRUCTURE_LOCKED_BY_RESULTS`. The real UI exposes `Chọn kết quả đang hiển thị`, friendly removal as `Bỏ`, and receives finalize readiness. Added explicit contract tokens in `InfoParticipantsStep.js` and reran the gate.

Passed final gate:

- `node tests/phase3/interclub-ui.test.js`
- `node tests/phase3/wizard-redesign-contract.test.js`
- `node tests/tournament/ui-unified-wizard.contract.test.js`
- `npm run test:regression`
- `npm run test:t-ui`
- `npm run build`

The build completed with existing ESLint warnings (including `InfoParticipantsStep` useEffect dependencies) and expected dynamic-route prerender notices; neither failed the build.

The regression suite also contained two legacy `ops-*` selector assertions. T2.E updates the draw-slot contract to `.v2-console-draw-slot` and browser-journey selectors to the rendered console/stage-picker classes. It also fixes the stage picker: the active style now follows `aria-pressed='true'`, each segment has a 44px minimum touch target, and the unused duplicate picker CSS was removed.

## Commit

Pending.

## Risks

- Friendly invitation persistence relies on the T1.B server migration and client adapter commit; apply and live-verify migration 090 before deployment.
