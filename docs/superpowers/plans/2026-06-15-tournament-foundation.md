# Tournament Module — Plan 1: Foundation (Migration + Engines)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dựng nền clean-slate cho module giải đấu: migration 7 bảng mới (drop bảng cũ) + format engine JS thuần (vòng tròn, knockout, match simple/MLP, orchestrator mix), test đầy đủ bằng node.

**Architecture:** Mô hình 2 trục — *schedule engine* (round_robin/knockout) sinh lịch + tính BXH từ kết quả đã chốt; *match engine* (simple/mlp) chốt thắng/thua 1 trận từ các ván; *orchestrator* ghép stage cho "mix". Engine là CommonJS thuần trong `lib/tournament/`, không chạm Supabase/React. Tham chiếu spec: `docs/superpowers/specs/2026-06-15-tournament-module-design.md`.

**Tech Stack:** Node CommonJS (engine + test), PostgreSQL/Supabase (migration SQL, áp tay). Test = node script thuần theo pattern `tests/*.test.js` của dự án.

---

## File Structure

- `database/migrations/015_tournament_module_v2.sql` — drop bảng giải cũ + tạo 7 bảng mới.
- `lib/tournament/seeding.js` — thứ tự gieo hạt bracket chuẩn (đệ quy).
- `lib/tournament/engines/roundRobin.js` — `generateSchedule`, `computeStandings`, `advance`.
- `lib/tournament/engines/knockout.js` — `generateSchedule`, `computeStandings`, `advance`.
- `lib/tournament/match/simple.js` — `resolveMatch`.
- `lib/tournament/match/mlp.js` — `resolveMatch`.
- `lib/tournament/engines/index.js` — registry chọn engine theo format.
- `lib/tournament/orchestrator.js` — ghép stage (mix): từ BXH stage trước → seeding stage sau.
- `tests/tournament/*.test.js` — test runtime thật từng module.

**Shape dữ liệu dùng chung (giữ nhất quán mọi task):**
- Entrant: `{ id, name, seed }` (seed nhỏ = mạnh hơn).
- Match do engine sinh: `{ round, group_label, bracket_slot, parent_slot, slot, entrant_a_id, entrant_b_id, order }` (giá trị không dùng = `null`).
- Match đã chốt (đưa vào `computeStandings`): `{ entrant_a_id, entrant_b_id, winner_entrant_id, points_a, points_b, games_a, games_b, status }`.
- Standings row: `{ entrant_id, played, won, lost, games_won, games_lost, points_for, points_against, diff, match_points, rank }`.
- `resolveMatch(match, games, config)` trả `{ winner_entrant_id, games_a, games_b, points_a, points_b, complete }`.

---

## Task 1: Migration clean-slate (7 bảng mới)

**Files:**
- Create: `database/migrations/015_tournament_module_v2.sql`
- Test: `tests/tournament/migration-015.test.js`

- [ ] **Step 1: Viết test contract (đọc SQL, assert nội dung)**

```js
// tests/tournament/migration-015.test.js
const fs = require('fs');
const path = require('path');
const root = path.join(__dirname, '..', '..');
const read = (f) => fs.readFileSync(path.join(root, f), 'utf8');
const assert = (c, m) => { if (!c) { console.error(`FAIL: ${m}`); process.exit(1); } };

const sql = read('database/migrations/015_tournament_module_v2.sql');

for (const t of ['tournament_teams', 'tournament_players', 'tournament_pairings', 'tournament_matches', 'tournament_settings']) {
  assert(new RegExp(`DROP TABLE IF EXISTS ${t}`, 'i').test(sql), `phải drop bảng cũ ${t}`);
}
for (const t of ['tournaments', 'tournament_stages', 'tournament_entrants', 'tournament_entrant_members', 'tournament_stage_entrants', 'tournament_matches', 'tournament_games']) {
  assert(new RegExp(`CREATE TABLE IF NOT EXISTS ${t}`, 'i').test(sql), `phải tạo bảng ${t}`);
}
assert(/entrant_type[\s\S]*?CHECK[\s\S]*?'pair'[\s\S]*?'team'/i.test(sql), 'tournaments.entrant_type check pair/team');
assert(/schedule_format[\s\S]*?'round_robin'[\s\S]*?'knockout'/i.test(sql), 'stage.schedule_format check');
assert(/match_format[\s\S]*?'simple'[\s\S]*?'mlp'/i.test(sql), 'stage.match_format check');
assert((sql.match(/group_id/gi) || []).length >= 7, 'mọi bảng có group_id');
assert(/DISABLE ROW LEVEL SECURITY/i.test(sql), 'RLS disable backstop');
console.log('migration-015 contract ok');
```

- [ ] **Step 2: Chạy test cho fail**

Run: `node tests/tournament/migration-015.test.js`
Expected: FAIL (`ENOENT` — chưa có file SQL).

- [ ] **Step 3: Viết migration**

```sql
-- database/migrations/015_tournament_module_v2.sql
-- Module giải đấu v2 (clean-slate). Drop bảng giải cũ, tạo mô hình stage 2 trục.
-- ÁP TAY trên Supabase. User đồng ý mất dữ liệu giải cũ.

DROP TABLE IF EXISTS tournament_matches CASCADE;
DROP TABLE IF EXISTS tournament_pairings CASCADE;
DROP TABLE IF EXISTS tournament_players CASCADE;
DROP TABLE IF EXISTS tournament_settings CASCADE;
DROP TABLE IF EXISTS tournament_teams CASCADE;
DROP TABLE IF EXISTS tournaments CASCADE;

CREATE TABLE IF NOT EXISTS tournaments (
  id            bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  group_id      integer NOT NULL,
  name          text NOT NULL,
  description   text,
  location      text,
  event_date    date,
  status        text NOT NULL DEFAULT 'draft' CHECK (status IN ('draft','active','completed')),
  entrant_type  text NOT NULL DEFAULT 'pair' CHECK (entrant_type IN ('pair','team')),
  public_slug   text,
  settings      jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at    timestamptz DEFAULT now(),
  updated_at    timestamptz DEFAULT now()
);
CREATE UNIQUE INDEX IF NOT EXISTS idx_tournaments_group_slug ON tournaments(group_id, public_slug);
ALTER TABLE tournaments DISABLE ROW LEVEL SECURITY;

CREATE TABLE IF NOT EXISTS tournament_stages (
  id              bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  group_id        integer NOT NULL,
  tournament_id   bigint NOT NULL REFERENCES tournaments(id) ON DELETE CASCADE,
  stage_order     integer NOT NULL DEFAULT 1,
  name            text NOT NULL,
  schedule_format text NOT NULL CHECK (schedule_format IN ('round_robin','knockout')),
  match_format    text NOT NULL DEFAULT 'simple' CHECK (match_format IN ('simple','mlp')),
  status          text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','active','completed')),
  config          jsonb NOT NULL DEFAULT '{}'::jsonb
);
ALTER TABLE tournament_stages DISABLE ROW LEVEL SECURITY;

CREATE TABLE IF NOT EXISTS tournament_entrants (
  id            bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  group_id      integer NOT NULL,
  tournament_id bigint NOT NULL REFERENCES tournaments(id) ON DELETE CASCADE,
  name          text NOT NULL,
  seed          integer,
  color         text
);
ALTER TABLE tournament_entrants DISABLE ROW LEVEL SECURITY;

CREATE TABLE IF NOT EXISTS tournament_entrant_members (
  id           bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  group_id     integer NOT NULL,
  entrant_id   bigint NOT NULL REFERENCES tournament_entrants(id) ON DELETE CASCADE,
  member_id    bigint,
  display_name text,
  gender       text CHECK (gender IN ('m','f'))
);
ALTER TABLE tournament_entrant_members DISABLE ROW LEVEL SECURITY;

CREATE TABLE IF NOT EXISTS tournament_stage_entrants (
  id            bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  group_id      integer NOT NULL,
  stage_id      bigint NOT NULL REFERENCES tournament_stages(id) ON DELETE CASCADE,
  entrant_id    bigint NOT NULL REFERENCES tournament_entrants(id) ON DELETE CASCADE,
  group_label   text,
  seed_in_stage integer
);
CREATE UNIQUE INDEX IF NOT EXISTS idx_stage_entrants_unique ON tournament_stage_entrants(group_id, stage_id, entrant_id);
ALTER TABLE tournament_stage_entrants DISABLE ROW LEVEL SECURITY;

CREATE TABLE IF NOT EXISTS tournament_matches (
  id                bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  group_id          integer NOT NULL,
  stage_id          bigint NOT NULL REFERENCES tournament_stages(id) ON DELETE CASCADE,
  round             integer NOT NULL DEFAULT 1,
  bracket_slot      integer,
  group_label       text,
  court             text,
  match_order       integer,
  entrant_a_id      bigint REFERENCES tournament_entrants(id) ON DELETE SET NULL,
  entrant_b_id      bigint REFERENCES tournament_entrants(id) ON DELETE SET NULL,
  status            text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','live','done')),
  winner_entrant_id bigint REFERENCES tournament_entrants(id) ON DELETE SET NULL,
  parent_match_id   bigint REFERENCES tournament_matches(id) ON DELETE SET NULL
);
ALTER TABLE tournament_matches DISABLE ROW LEVEL SECURITY;

CREATE TABLE IF NOT EXISTS tournament_games (
  id                bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  group_id          integer NOT NULL,
  match_id          bigint NOT NULL REFERENCES tournament_matches(id) ON DELETE CASCADE,
  game_no           integer NOT NULL DEFAULT 1,
  kind              text NOT NULL DEFAULT 'game'
                      CHECK (kind IN ('game','womens','mens','mixed1','mixed2','dreambreaker')),
  score_a           integer NOT NULL DEFAULT 0,
  score_b           integer NOT NULL DEFAULT 0,
  winner_entrant_id bigint REFERENCES tournament_entrants(id) ON DELETE SET NULL,
  lineup            jsonb NOT NULL DEFAULT '{}'::jsonb
);
ALTER TABLE tournament_games DISABLE ROW LEVEL SECURITY;

COMMENT ON TABLE tournaments IS 'Giải đấu (module v2, mô hình stage 2 trục)';
COMMENT ON COLUMN tournament_stages.schedule_format IS 'Trục xếp lịch: round_robin, knockout';
COMMENT ON COLUMN tournament_stages.match_format IS 'Trục tính trận: simple (best-of-N), mlp (ván con)';
```

- [ ] **Step 4: Chạy test cho pass**

Run: `node tests/tournament/migration-015.test.js`
Expected: PASS — `migration-015 contract ok`.

- [ ] **Step 5: Đăng ký script + commit**

Thêm vào `package.json` scripts: `"test:t-migration": "node tests/tournament/migration-015.test.js"`.

```bash
git add database/migrations/015_tournament_module_v2.sql tests/tournament/migration-015.test.js package.json
git commit -m "feat(tournament): clean-slate migration 015 (7-table v2 schema)"
```

> **Lưu ý vận hành:** migration này drop bảng prod — KHÔNG tự áp. Ghi chú để user áp tay + deploy theo `pickhub-engineering`.

---

## Task 2: Seeding helper (thứ tự gieo hạt bracket)

**Files:**
- Create: `lib/tournament/seeding.js`
- Test: `tests/tournament/seeding.test.js`

- [ ] **Step 1: Viết test fail**

```js
// tests/tournament/seeding.test.js
const { seedOrder } = require('../../lib/tournament/seeding');
const assert = (c, m) => { if (!c) { console.error(`FAIL: ${m}`); process.exit(1); } };
const eq = (a, b, m) => assert(JSON.stringify(a) === JSON.stringify(b), `${m} — got ${JSON.stringify(a)}`);

eq(seedOrder(2), [1, 2], 'B=2');
eq(seedOrder(4), [1, 4, 3, 2], 'B=4');
eq(seedOrder(8), [1, 8, 5, 4, 3, 6, 7, 2], 'B=8');
assert(seedOrder(16).length === 16 && seedOrder(16)[0] === 1 && seedOrder(16)[1] === 16, 'B=16 đầu/cuối');
console.log('seeding ok');
```

- [ ] **Step 2: Chạy fail**

Run: `node tests/tournament/seeding.test.js`
Expected: FAIL (`Cannot find module`).

- [ ] **Step 3: Implement**

```js
// lib/tournament/seeding.js
// Thứ tự slot gieo hạt chuẩn cho bracket kích thước B (lũy thừa 2).
// seedOrder(8) -> [1,8,5,4,3,6,7,2]: hạt 1 và 2 ở hai nửa, chỉ gặp ở CK.
function seedOrder(bracketSize) {
  let rounds = Math.log2(bracketSize);
  if (!Number.isInteger(rounds)) throw new Error('bracketSize phải là lũy thừa 2');
  let order = [1, 2];
  for (let r = 1; r < rounds; r++) {
    const next = [];
    const sum = order.length * 2 + 1;
    for (const x of order) {
      next.push(x);
      next.push(sum - x);
    }
    order = next;
  }
  return order;
}

function nextPowerOfTwo(n) {
  let p = 1;
  while (p < n) p *= 2;
  return p;
}

module.exports = { seedOrder, nextPowerOfTwo };
```

- [ ] **Step 4: Chạy pass**

Run: `node tests/tournament/seeding.test.js`
Expected: PASS — `seeding ok`.

- [ ] **Step 5: Commit**

```bash
git add lib/tournament/seeding.js tests/tournament/seeding.test.js
git commit -m "feat(tournament): bracket seeding order helper"
```

---

## Task 3: Match engine `simple` (best-of-N)

**Files:**
- Create: `lib/tournament/match/simple.js`
- Test: `tests/tournament/match-simple.test.js`

- [ ] **Step 1: Viết test fail**

```js
// tests/tournament/match-simple.test.js
const { resolveMatch } = require('../../lib/tournament/match/simple');
const assert = (c, m) => { if (!c) { console.error(`FAIL: ${m}`); process.exit(1); } };

const match = { entrant_a_id: 10, entrant_b_id: 20 };
// best-of-3: A thắng 2-1
let r = resolveMatch(match, [
  { score_a: 11, score_b: 9 },
  { score_a: 7, score_b: 11 },
  { score_a: 11, score_b: 8 },
], { bestOf: 3 });
assert(r.complete === true, 'đủ ván -> complete');
assert(r.winner_entrant_id === 10, 'A thắng 2-1');
assert(r.games_a === 2 && r.games_b === 1, 'tỉ số ván 2-1');
assert(r.points_a === 29 && r.points_b === 28, 'tổng điểm');

// chưa đủ kết quả -> chưa complete, chưa có winner
let r2 = resolveMatch(match, [{ score_a: 11, score_b: 9 }], { bestOf: 3 });
assert(r2.complete === false && r2.winner_entrant_id === null, 'mới 1 ván chưa xong');
console.log('match-simple ok');
```

- [ ] **Step 2: Chạy fail**

Run: `node tests/tournament/match-simple.test.js`
Expected: FAIL (`Cannot find module`).

- [ ] **Step 3: Implement**

```js
// lib/tournament/match/simple.js
// Chốt 1 trận best-of-N ván. games: [{score_a, score_b}].
function resolveMatch(match, games, config = {}) {
  const bestOf = config.bestOf || 3;
  const needed = Math.floor(bestOf / 2) + 1;
  let gamesA = 0, gamesB = 0, pointsA = 0, pointsB = 0;
  for (const g of games) {
    const a = Number(g.score_a) || 0;
    const b = Number(g.score_b) || 0;
    pointsA += a; pointsB += b;
    if (a > b) gamesA++; else if (b > a) gamesB++;
  }
  const complete = gamesA >= needed || gamesB >= needed;
  let winner = null;
  if (complete) winner = gamesA > gamesB ? match.entrant_a_id : match.entrant_b_id;
  return {
    winner_entrant_id: winner,
    games_a: gamesA, games_b: gamesB,
    points_a: pointsA, points_b: pointsB,
    complete,
  };
}

module.exports = { resolveMatch };
```

- [ ] **Step 4: Chạy pass**

Run: `node tests/tournament/match-simple.test.js`
Expected: PASS — `match-simple ok`.

- [ ] **Step 5: Commit**

```bash
git add lib/tournament/match/simple.js tests/tournament/match-simple.test.js
git commit -m "feat(tournament): simple best-of-N match engine"
```

---

## Task 4: Match engine `mlp` (ván con + Dreambreaker)

**Files:**
- Create: `lib/tournament/match/mlp.js`
- Test: `tests/tournament/match-mlp.test.js`

- [ ] **Step 1: Viết test fail**

```js
// tests/tournament/match-mlp.test.js
const { resolveMatch } = require('../../lib/tournament/match/mlp');
const assert = (c, m) => { if (!c) { console.error(`FAIL: ${m}`); process.exit(1); } };

const match = { entrant_a_id: 1, entrant_b_id: 2 };
const cfg = { subMatches: ['womens', 'mens', 'mixed1', 'mixed2'], dreambreaker: true };

// A thắng 3 ván con -> thắng luôn (không cần dreambreaker)
let r = resolveMatch(match, [
  { kind: 'womens', score_a: 21, score_b: 15 },
  { kind: 'mens', score_a: 18, score_b: 21 },
  { kind: 'mixed1', score_a: 21, score_b: 19 },
  { kind: 'mixed2', score_a: 21, score_b: 10 },
], cfg);
assert(r.complete && r.winner_entrant_id === 1, 'A thắng 3-1');
assert(r.games_a === 3 && r.games_b === 1, 'ván con 3-1');

// Hòa 2-2 -> cần dreambreaker
let r2 = resolveMatch(match, [
  { kind: 'womens', score_a: 21, score_b: 15 },
  { kind: 'mens', score_a: 15, score_b: 21 },
  { kind: 'mixed1', score_a: 21, score_b: 19 },
  { kind: 'mixed2', score_a: 10, score_b: 21 },
], cfg);
assert(r2.complete === false && r2.winner_entrant_id === null, 'hòa 2-2 chờ dreambreaker');

// Dreambreaker quyết định
let r3 = resolveMatch(match, [
  { kind: 'womens', score_a: 21, score_b: 15 },
  { kind: 'mens', score_a: 15, score_b: 21 },
  { kind: 'mixed1', score_a: 21, score_b: 19 },
  { kind: 'mixed2', score_a: 10, score_b: 21 },
  { kind: 'dreambreaker', score_a: 21, score_b: 18 },
], cfg);
assert(r3.complete && r3.winner_entrant_id === 1, 'dreambreaker A thắng');
console.log('match-mlp ok');
```

- [ ] **Step 2: Chạy fail**

Run: `node tests/tournament/match-mlp.test.js`
Expected: FAIL.

- [ ] **Step 3: Implement**

```js
// lib/tournament/match/mlp.js
// Chốt 1 trận MLP: cộng ván con thắng; hòa -> Dreambreaker.
function resolveMatch(match, games, config = {}) {
  const subs = config.subMatches || ['womens', 'mens', 'mixed1', 'mixed2'];
  const useDreambreaker = config.dreambreaker !== false;
  let winsA = 0, winsB = 0, pointsA = 0, pointsB = 0;
  let dbWinner = null;

  for (const g of games) {
    const a = Number(g.score_a) || 0;
    const b = Number(g.score_b) || 0;
    pointsA += a; pointsB += b;
    if (g.kind === 'dreambreaker') {
      dbWinner = a > b ? match.entrant_a_id : (b > a ? match.entrant_b_id : null);
      continue;
    }
    if (a > b) winsA++; else if (b > a) winsB++;
  }

  const playedSubs = winsA + winsB;
  const allSubsDone = playedSubs >= subs.length;
  let winner = null, complete = false;

  if (allSubsDone && winsA !== winsB) {
    winner = winsA > winsB ? match.entrant_a_id : match.entrant_b_id;
    complete = true;
  } else if (allSubsDone && winsA === winsB && useDreambreaker && dbWinner) {
    winner = dbWinner;
    complete = true;
  }

  return {
    winner_entrant_id: winner,
    games_a: winsA, games_b: winsB,
    points_a: pointsA, points_b: pointsB,
    complete,
  };
}

module.exports = { resolveMatch };
```

- [ ] **Step 4: Chạy pass**

Run: `node tests/tournament/match-mlp.test.js`
Expected: PASS — `match-mlp ok`.

- [ ] **Step 5: Commit**

```bash
git add lib/tournament/match/mlp.js tests/tournament/match-mlp.test.js
git commit -m "feat(tournament): MLP match engine with dreambreaker"
```

---

## Task 5: Round-robin — `generateSchedule` (1 bảng, circle method)

**Files:**
- Create: `lib/tournament/engines/roundRobin.js`
- Test: `tests/tournament/round-robin-schedule.test.js`

- [ ] **Step 1: Viết test fail**

```js
// tests/tournament/round-robin-schedule.test.js
const { generateSchedule } = require('../../lib/tournament/engines/roundRobin');
const assert = (c, m) => { if (!c) { console.error(`FAIL: ${m}`); process.exit(1); } };

const entrants = [1, 2, 3, 4].map((id) => ({ id, name: 'E' + id, seed: id }));
const matches = generateSchedule({ config: {} }, entrants, 42);

// C(4,2) = 6 trận, 3 vòng
assert(matches.length === 6, `4 đội -> 6 trận, got ${matches.length}`);
const rounds = new Set(matches.map((m) => m.round));
assert(rounds.size === 3, '3 vòng');
// mỗi cặp xuất hiện đúng 1 lần
const pairs = matches.map((m) => [m.entrant_a_id, m.entrant_b_id].sort((a, b) => a - b).join('-'));
assert(new Set(pairs).size === 6, 'không cặp trùng');
// không tự đấu, không có bye (chẵn)
assert(matches.every((m) => m.entrant_a_id && m.entrant_b_id && m.entrant_a_id !== m.entrant_b_id), 'cặp hợp lệ');

// lẻ -> bye: 5 đội, C(5,2)=10 trận thực (bỏ trận gặp BYE)
const odd = [1, 2, 3, 4, 5].map((id) => ({ id, name: 'E' + id, seed: id }));
const m5 = generateSchedule({ config: {} }, odd, 1);
assert(m5.length === 10, `5 đội -> 10 trận, got ${m5.length}`);
console.log('round-robin-schedule ok');
```

- [ ] **Step 2: Chạy fail**

Run: `node tests/tournament/round-robin-schedule.test.js`
Expected: FAIL (`Cannot find module`).

- [ ] **Step 3: Implement (chỉ generateSchedule cho 1 bảng; computeStandings/advance thêm ở task sau)**

```js
// lib/tournament/engines/roundRobin.js
// Engine vòng tròn. Hàm thuần, deterministic; random dùng seed.

// PRNG nhỏ (mulberry32) để shuffle tái lập được.
function rng(seed) {
  let a = seed >>> 0;
  return function () {
    a |= 0; a = (a + 0x6D2B79F5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function shuffle(arr, seed) {
  const r = rng(seed);
  const a = arr.slice();
  for (let i = a.length - 1; i > 0; i--) {
    const j = Math.floor(r() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a;
}

// Circle method cho 1 danh sách entrant -> [{round, entrant_a_id, entrant_b_id}]
function circleMethod(ids) {
  const arr = ids.slice();
  if (arr.length % 2 === 1) arr.push(null); // BYE ảo
  const n = arr.length;
  const rounds = [];
  for (let r = 0; r < n - 1; r++) {
    const pairs = [];
    for (let i = 0; i < n / 2; i++) {
      const a = arr[i];
      const b = arr[n - 1 - i];
      if (a !== null && b !== null) pairs.push([a, b]);
    }
    rounds.push(pairs);
    // xoay: giữ phần tử 0, dịch còn lại
    arr.splice(1, 0, arr.pop());
  }
  return rounds;
}

function generateSchedule(stage, entrants, seed = 1) {
  const config = stage.config || {};
  const shuffled = config.shuffle === false ? entrants.slice() : shuffle(entrants, seed);
  const ids = shuffled.map((e) => e.id);
  const rounds = circleMethod(ids);
  const matches = [];
  let order = 0;
  rounds.forEach((pairs, ri) => {
    pairs.forEach(([a, b]) => {
      matches.push({
        round: ri + 1,
        group_label: 'A',
        bracket_slot: null,
        parent_slot: null,
        slot: null,
        entrant_a_id: a,
        entrant_b_id: b,
        order: order++,
      });
    });
  });
  return matches;
}

module.exports = { generateSchedule };
```

- [ ] **Step 4: Chạy pass**

Run: `node tests/tournament/round-robin-schedule.test.js`
Expected: PASS — `round-robin-schedule ok`.

- [ ] **Step 5: Commit**

```bash
git add lib/tournament/engines/roundRobin.js tests/tournament/round-robin-schedule.test.js
git commit -m "feat(tournament): round-robin schedule (circle method + bye)"
```

---

## Task 6: Round-robin — chia bảng (snake) trong `generateSchedule`

**Files:**
- Modify: `lib/tournament/engines/roundRobin.js`
- Test: `tests/tournament/round-robin-groups.test.js`

- [ ] **Step 1: Viết test fail**

```js
// tests/tournament/round-robin-groups.test.js
const { generateSchedule, splitGroups } = require('../../lib/tournament/engines/roundRobin');
const assert = (c, m) => { if (!c) { console.error(`FAIL: ${m}`); process.exit(1); } };

const entrants = [1, 2, 3, 4, 5, 6, 7, 8].map((id) => ({ id, name: 'E' + id, seed: id }));
// snake 2 bảng: A=[1,4,5,8], B=[2,3,6,7]
const groups = splitGroups(entrants, 2);
assert(groups.A.map((e) => e.id).join(',') === '1,4,5,8', `bảng A snake, got ${groups.A.map((e) => e.id)}`);
assert(groups.B.map((e) => e.id).join(',') === '2,3,6,7', `bảng B snake, got ${groups.B.map((e) => e.id)}`);

const matches = generateSchedule({ config: { groupCount: 2 } }, entrants, 7);
// mỗi bảng 4 đội -> 6 trận, tổng 12
assert(matches.length === 12, `2 bảng x 6 = 12 trận, got ${matches.length}`);
assert(matches.some((m) => m.group_label === 'A') && matches.some((m) => m.group_label === 'B'), 'có nhãn 2 bảng');
console.log('round-robin-groups ok');
```

- [ ] **Step 2: Chạy fail**

Run: `node tests/tournament/round-robin-groups.test.js`
Expected: FAIL (`splitGroups is not a function`).

- [ ] **Step 3: Bổ sung `splitGroups` + dùng trong generateSchedule**

Thêm vào `lib/tournament/engines/roundRobin.js` (trước `module.exports`):

```js
// Chia entrant thành N bảng kiểu rắn (snake) theo seed để cân sức.
function splitGroups(entrants, groupCount) {
  const sorted = entrants.slice().sort((a, b) => (a.seed || 0) - (b.seed || 0));
  const labels = Array.from({ length: groupCount }, (_, i) => String.fromCharCode(65 + i));
  const groups = {};
  labels.forEach((l) => (groups[l] = []));
  let idx = 0, dir = 1;
  for (const e of sorted) {
    groups[labels[idx]].push(e);
    if (dir === 1 && idx === groupCount - 1) dir = -1;
    else if (dir === -1 && idx === 0) dir = 1;
    else idx += dir;
  }
  return groups;
}
```

Sửa `generateSchedule` để hỗ trợ nhiều bảng:

```js
function generateSchedule(stage, entrants, seed = 1) {
  const config = stage.config || {};
  const groupCount = config.groupCount || 1;
  let order = 0;
  const matches = [];

  const buildGroup = (members, label) => {
    const shuffled = config.shuffle === false ? members.slice() : shuffle(members, seed + label.charCodeAt(0));
    const rounds = circleMethod(shuffled.map((e) => e.id));
    rounds.forEach((pairs, ri) => {
      pairs.forEach(([a, b]) => {
        matches.push({
          round: ri + 1, group_label: label, bracket_slot: null,
          parent_slot: null, slot: null,
          entrant_a_id: a, entrant_b_id: b, order: order++,
        });
      });
    });
  };

  if (groupCount <= 1) {
    buildGroup(entrants, 'A');
  } else {
    const groups = splitGroups(entrants, groupCount);
    Object.keys(groups).forEach((label) => buildGroup(groups[label], label));
  }
  return matches;
}
```

Cập nhật `module.exports`:

```js
module.exports = { generateSchedule, splitGroups };
```

- [ ] **Step 4: Chạy cả 2 test round-robin cho pass**

Run: `node tests/tournament/round-robin-groups.test.js && node tests/tournament/round-robin-schedule.test.js`
Expected: PASS cả hai (`round-robin-groups ok`, `round-robin-schedule ok`).

- [ ] **Step 5: Commit**

```bash
git add lib/tournament/engines/roundRobin.js tests/tournament/round-robin-groups.test.js
git commit -m "feat(tournament): round-robin snake group split"
```

---

## Task 7: Round-robin — `computeStandings` + tie-break + `advance`

**Files:**
- Modify: `lib/tournament/engines/roundRobin.js`
- Test: `tests/tournament/round-robin-standings.test.js`

- [ ] **Step 1: Viết test fail**

```js
// tests/tournament/round-robin-standings.test.js
const { computeStandings, advance } = require('../../lib/tournament/engines/roundRobin');
const assert = (c, m) => { if (!c) { console.error(`FAIL: ${m}`); process.exit(1); } };

const entrants = [1, 2, 3].map((id) => ({ id, name: 'E' + id, seed: id }));
// resolved matches (round-robin 3 đội): 1>2, 1>3, 2>3
const matches = [
  { entrant_a_id: 1, entrant_b_id: 2, winner_entrant_id: 1, points_a: 22, points_b: 18, games_a: 2, games_b: 0, status: 'done', group_label: 'A' },
  { entrant_a_id: 1, entrant_b_id: 3, winner_entrant_id: 1, points_a: 22, points_b: 15, games_a: 2, games_b: 0, status: 'done', group_label: 'A' },
  { entrant_a_id: 2, entrant_b_id: 3, winner_entrant_id: 2, points_a: 21, points_b: 19, games_a: 2, games_b: 1, status: 'done', group_label: 'A' },
];
const standings = computeStandings({ config: { winPoints: 2 } }, entrants, matches);
assert(standings[0].entrant_id === 1 && standings[0].rank === 1, 'E1 nhất');
assert(standings[1].entrant_id === 2 && standings[1].rank === 2, 'E2 nhì');
assert(standings[2].entrant_id === 3 && standings[2].rank === 3, 'E3 ba');
assert(standings[0].won === 2 && standings[0].match_points === 4, 'E1 thắng 2, 4 điểm');

// advance: top-2 -> seed knockout
const adv = advance({ config: { groupCount: 1, advancePerGroup: 2 } }, standings);
assert(adv.length === 2 && adv[0].entrant_id === 1 && adv[0].seed_in_stage === 1, 'đi tiếp 2 đội, seed đúng');
console.log('round-robin-standings ok');
```

- [ ] **Step 2: Chạy fail**

Run: `node tests/tournament/round-robin-standings.test.js`
Expected: FAIL (`computeStandings is not a function`).

- [ ] **Step 3: Bổ sung `computeStandings` + `advance`**

Thêm vào `lib/tournament/engines/roundRobin.js`:

```js
function computeStandings(stage, entrants, matches) {
  const config = stage.config || {};
  const winPoints = config.winPoints != null ? config.winPoints : 2;
  const lossPoints = config.lossPoints != null ? config.lossPoints : 0;

  const rows = new Map();
  for (const e of entrants) {
    rows.set(e.id, {
      entrant_id: e.id, played: 0, won: 0, lost: 0,
      games_won: 0, games_lost: 0, points_for: 0, points_against: 0,
      diff: 0, match_points: 0, group_label: null, seed: e.seed || 0,
    });
  }
  const h2h = new Map(); // "a-b" -> winner

  for (const m of matches) {
    if (m.status !== 'done' || !m.winner_entrant_id) continue;
    const ra = rows.get(m.entrant_a_id);
    const rb = rows.get(m.entrant_b_id);
    if (!ra || !rb) continue;
    ra.group_label = rb.group_label = m.group_label || ra.group_label;
    ra.played++; rb.played++;
    ra.games_won += m.games_a; ra.games_lost += m.games_b;
    rb.games_won += m.games_b; rb.games_lost += m.games_a;
    ra.points_for += m.points_a; ra.points_against += m.points_b;
    rb.points_for += m.points_b; rb.points_against += m.points_a;
    if (m.winner_entrant_id === m.entrant_a_id) { ra.won++; rb.lost++; }
    else { rb.won++; ra.lost++; }
    h2h.set([m.entrant_a_id, m.entrant_b_id].sort((x, y) => x - y).join('-'), m.winner_entrant_id);
  }

  const all = [];
  for (const row of rows.values()) {
    row.diff = row.points_for - row.points_against;
    row.match_points = row.won * winPoints + row.lost * lossPoints;
    all.push(row);
  }

  // Sắp theo bảng rồi tie-break trong bảng
  const cmp = (a, b) => {
    if (b.match_points !== a.match_points) return b.match_points - a.match_points;
    if (b.diff !== a.diff) return b.diff - a.diff;
    const key = [a.entrant_id, b.entrant_id].sort((x, y) => x - y).join('-');
    const w = h2h.get(key);
    if (w) return w === a.entrant_id ? -1 : 1;
    if (b.points_for !== a.points_for) return b.points_for - a.points_for;
    return (a.seed || 0) - (b.seed || 0);
  };
  all.sort((a, b) => {
    if ((a.group_label || '') !== (b.group_label || '')) return (a.group_label || '').localeCompare(b.group_label || '');
    return cmp(a, b);
  });

  // Gán rank trong từng bảng
  const byGroup = {};
  for (const row of all) (byGroup[row.group_label || 'A'] ||= []).push(row);
  for (const label of Object.keys(byGroup)) {
    byGroup[label].forEach((row, i) => { row.rank = i + 1; });
  }
  return all;
}

// Lấy top-K mỗi bảng làm seeding cho stage knockout kế.
function advance(stage, standings) {
  const config = stage.config || {};
  const k = config.advancePerGroup || 2;
  const byGroup = {};
  for (const row of standings) (byGroup[row.group_label || 'A'] ||= []).push(row);
  for (const label of Object.keys(byGroup)) byGroup[label].sort((a, b) => a.rank - b.rank);

  // Xếp seed cross-bảng: vòng theo rank (tất cả hạng 1 trước, rồi hạng 2…)
  const labels = Object.keys(byGroup).sort();
  const advanced = [];
  for (let r = 0; r < k; r++) {
    for (const label of labels) {
      const row = byGroup[label][r];
      if (row) advanced.push({ entrant_id: row.entrant_id, from_group: label, from_rank: r + 1 });
    }
  }
  return advanced.map((a, i) => ({ ...a, seed_in_stage: i + 1 }));
}

module.exports = { generateSchedule, splitGroups, computeStandings, advance };
```

- [ ] **Step 4: Chạy pass**

Run: `node tests/tournament/round-robin-standings.test.js`
Expected: PASS — `round-robin-standings ok`.

- [ ] **Step 5: Commit**

```bash
git add lib/tournament/engines/roundRobin.js tests/tournament/round-robin-standings.test.js
git commit -m "feat(tournament): round-robin standings, tie-break, advance"
```

---

## Task 8: Knockout — `generateSchedule` (bracket + bye + parent link)

**Files:**
- Create: `lib/tournament/engines/knockout.js`
- Test: `tests/tournament/knockout-schedule.test.js`

- [ ] **Step 1: Viết test fail**

```js
// tests/tournament/knockout-schedule.test.js
const { generateSchedule } = require('../../lib/tournament/engines/knockout');
const assert = (c, m) => { if (!c) { console.error(`FAIL: ${m}`); process.exit(1); } };

// 8 đội -> 7 trận (4+2+1), 3 vòng
const e8 = [1, 2, 3, 4, 5, 6, 7, 8].map((id) => ({ id, name: 'E' + id, seed: id }));
const m8 = generateSchedule({ config: {} }, e8, 1);
assert(m8.length === 7, `8 đội -> 7 trận, got ${m8.length}`);
const r1 = m8.filter((m) => m.round === 1);
assert(r1.length === 4, '4 trận vòng 1');
// seed 1 gặp seed 8 ở vòng 1 (theo seedOrder)
const top = r1.find((m) => m.entrant_a_id === 1 || m.entrant_b_id === 1);
assert([top.entrant_a_id, top.entrant_b_id].includes(8), 'seed1 vs seed8');
// trận vòng 2 có parent_slot trỏ về vòng 1
const r2 = m8.filter((m) => m.round === 2);
assert(r2.every((m) => m.parent_slot == null) === false, 'vòng>1 có liên kết slot');

// 6 đội -> bracket 8, 2 bye cho seed 1,2
const e6 = [1, 2, 3, 4, 5, 6].map((id) => ({ id, name: 'E' + id, seed: id }));
const m6 = generateSchedule({ config: {} }, e6, 1);
const r1b = m6.filter((m) => m.round === 1);
// 2 đội mạnh nhất được bye -> vòng 1 chỉ có trận giữa các đội còn lại; có trận chứa null (bye) bị bỏ
assert(r1b.every((m) => m.entrant_a_id && m.entrant_b_id), 'vòng 1 không chứa bye rỗng');
assert(m6.filter((m) => m.round === 1).length === 2, '6 đội -> 2 trận vòng 1');
console.log('knockout-schedule ok');
```

- [ ] **Step 2: Chạy fail**

Run: `node tests/tournament/knockout-schedule.test.js`
Expected: FAIL (`Cannot find module`).

- [ ] **Step 3: Implement**

```js
// lib/tournament/engines/knockout.js
const { seedOrder, nextPowerOfTwo } = require('../seeding');

// Sinh bracket single-elimination. entrants có seed (nhỏ = mạnh).
// Trả mảng match {round, bracket_slot, parent_slot, slot, entrant_a_id, entrant_b_id, order}.
// slot = chỉ số duy nhất trong stage; parent_slot = slot trận kế mà winner đi vào.
function generateSchedule(stage, entrants, seed = 1) {
  const sorted = entrants.slice().sort((a, b) => (a.seed || 0) - (b.seed || 0));
  const n = sorted.length;
  const B = nextPowerOfTwo(Math.max(2, n));
  const order = seedOrder(B); // vị trí slot -> hạt giống (1-based)
  // map vị trí bracket -> entrant id (hoặc null nếu là bye)
  const positions = order.map((seedRank) => sorted[seedRank - 1] ? sorted[seedRank - 1].id : null);

  const rounds = Math.log2(B);
  const matches = [];
  let slot = 0;
  let orderCounter = 0;

  // Vòng 1: ghép cặp positions[0]-[1], [2]-[3], ...
  // Lưu winnerSlot theo từng "khe" để vòng sau nối parent.
  let prevSlots = []; // slot của trận vòng trước theo thứ tự khe
  let advancedByBye = []; // entrant id tự qua vòng (gặp bye)

  // Vòng 1
  const r1Khe = []; // mỗi khe: {matchSlot|null, entrantId|null}
  for (let i = 0; i < B; i += 2) {
    const a = positions[i];
    const b = positions[i + 1];
    if (a && b) {
      const s = slot++;
      matches.push({
        round: 1, bracket_slot: i / 2, parent_slot: null, slot: s,
        group_label: null, entrant_a_id: a, entrant_b_id: b, order: orderCounter++,
      });
      r1Khe.push({ matchSlot: s, entrantId: null });
    } else {
      // một bên bye -> bên còn lại tự qua vòng 2
      r1Khe.push({ matchSlot: null, entrantId: a || b });
    }
  }

  // Các vòng sau
  let prevKhe = r1Khe;
  for (let r = 2; r <= rounds; r++) {
    const curKhe = [];
    for (let i = 0; i < prevKhe.length; i += 2) {
      const left = prevKhe[i];
      const right = prevKhe[i + 1];
      const s = slot++;
      const entrant_a_id = left.entrantId || null;
      const entrant_b_id = right.entrantId || null;
      const match = {
        round: r, bracket_slot: i / 2, parent_slot: null, slot: s,
        group_label: null, entrant_a_id, entrant_b_id, order: orderCounter++,
      };
      matches.push(match);
      // nối parent: winner trận vòng trước (nếu có matchSlot) đi vào slot này
      if (left.matchSlot != null) {
        matches.find((m) => m.slot === left.matchSlot).parent_slot = s;
      }
      if (right.matchSlot != null) {
        matches.find((m) => m.slot === right.matchSlot).parent_slot = s;
      }
      curKhe.push({ matchSlot: s, entrantId: null });
    }
    prevKhe = curKhe;
  }

  return matches;
}

module.exports = { generateSchedule };
```

- [ ] **Step 4: Chạy pass**

Run: `node tests/tournament/knockout-schedule.test.js`
Expected: PASS — `knockout-schedule ok`.

- [ ] **Step 5: Commit**

```bash
git add lib/tournament/engines/knockout.js tests/tournament/knockout-schedule.test.js
git commit -m "feat(tournament): knockout bracket schedule with byes + parent links"
```

---

## Task 9: Knockout — `computeStandings` (thứ hạng) + `advance`

**Files:**
- Modify: `lib/tournament/engines/knockout.js`
- Test: `tests/tournament/knockout-standings.test.js`

- [ ] **Step 1: Viết test fail**

```js
// tests/tournament/knockout-standings.test.js
const { computeStandings, advance } = require('../../lib/tournament/engines/knockout');
const assert = (c, m) => { if (!c) { console.error(`FAIL: ${m}`); process.exit(1); } };

const entrants = [1, 2, 3, 4].map((id) => ({ id, name: 'E' + id, seed: id }));
// bracket 4: vòng1 (1v4, 3v2), CK winner1 v winner3
const matches = [
  { round: 1, slot: 0, parent_slot: 2, entrant_a_id: 1, entrant_b_id: 4, winner_entrant_id: 1, status: 'done' },
  { round: 1, slot: 1, parent_slot: 2, entrant_a_id: 3, entrant_b_id: 2, winner_entrant_id: 2, status: 'done' },
  { round: 2, slot: 2, parent_slot: null, entrant_a_id: 1, entrant_b_id: 2, winner_entrant_id: 1, status: 'done' },
];
const st = computeStandings({ config: {} }, entrants, matches);
assert(st[0].entrant_id === 1 && st[0].rank === 1, 'vô địch E1');
assert(st[1].entrant_id === 2 && st[1].rank === 2, 'á quân E2');
// E3, E4 thua vòng 1 -> hạng đồng 3
assert(st.find((r) => r.entrant_id === 3).rank === 3 && st.find((r) => r.entrant_id === 4).rank === 3, 'thua vòng 1 = hạng 3');

const champ = advance({ config: {} }, st);
assert(champ.length === 1 && champ[0].entrant_id === 1, 'advance trả nhà vô địch');
console.log('knockout-standings ok');
```

- [ ] **Step 2: Chạy fail**

Run: `node tests/tournament/knockout-standings.test.js`
Expected: FAIL (`computeStandings is not a function`).

- [ ] **Step 3: Bổ sung `computeStandings` + `advance`**

Thêm vào `lib/tournament/engines/knockout.js`:

```js
// Thứ hạng knockout: vô địch -> á quân -> theo vòng bị loại (muộn hơn = hạng cao hơn).
function computeStandings(stage, entrants, matches) {
  const maxRound = matches.reduce((mx, m) => Math.max(mx, m.round), 0);
  // vòng bị loại của mỗi entrant (nếu thua); người vô địch = maxRound+1
  const eliminatedRound = new Map();
  let champion = null, runnerUp = null;
  for (const m of matches) {
    if (m.status !== 'done' || !m.winner_entrant_id) continue;
    const loser = m.winner_entrant_id === m.entrant_a_id ? m.entrant_b_id : m.entrant_a_id;
    if (loser) eliminatedRound.set(loser, m.round);
    if (m.round === maxRound) {
      champion = m.winner_entrant_id;
      runnerUp = loser;
    }
  }

  const rows = entrants.map((e) => {
    let exitRound;
    if (e.id === champion) exitRound = maxRound + 1;
    else exitRound = eliminatedRound.has(e.id) ? eliminatedRound.get(e.id) : 0;
    return { entrant_id: e.id, exit_round: exitRound, seed: e.seed || 0, rank: 0 };
  });
  // sắp: exit_round giảm dần; cùng vòng -> seed nhỏ trước
  rows.sort((a, b) => (b.exit_round - a.exit_round) || (a.seed - b.seed));
  // gán rank: champion=1, runnerUp=2, cùng exit_round (thua cùng vòng) chia sẻ hạng
  let rank = 1;
  for (let i = 0; i < rows.length; i++) {
    if (i > 0 && rows[i].exit_round !== rows[i - 1].exit_round) rank = i + 1;
    rows[i].rank = rank;
  }
  return rows;
}

function advance(stage, standings) {
  const champ = standings.find((r) => r.rank === 1);
  return champ ? [{ entrant_id: champ.entrant_id, seed_in_stage: 1 }] : [];
}

module.exports = { generateSchedule, computeStandings, advance };
```

- [ ] **Step 4: Chạy pass**

Run: `node tests/tournament/knockout-standings.test.js && node tests/tournament/knockout-schedule.test.js`
Expected: PASS cả hai.

- [ ] **Step 5: Commit**

```bash
git add lib/tournament/engines/knockout.js tests/tournament/knockout-standings.test.js
git commit -m "feat(tournament): knockout standings (placement) + advance"
```

---

## Task 10: Registry `engines/index.js`

**Files:**
- Create: `lib/tournament/engines/index.js`
- Test: `tests/tournament/registry.test.js`

- [ ] **Step 1: Viết test fail**

```js
// tests/tournament/registry.test.js
const { getScheduleEngine, getMatchEngine } = require('../../lib/tournament/engines');
const assert = (c, m) => { if (!c) { console.error(`FAIL: ${m}`); process.exit(1); } };

assert(typeof getScheduleEngine('round_robin').generateSchedule === 'function', 'round_robin engine');
assert(typeof getScheduleEngine('knockout').generateSchedule === 'function', 'knockout engine');
assert(typeof getMatchEngine('simple').resolveMatch === 'function', 'simple match');
assert(typeof getMatchEngine('mlp').resolveMatch === 'function', 'mlp match');
let threw = false;
try { getScheduleEngine('xyz'); } catch (e) { threw = true; }
assert(threw, 'format lạ -> ném lỗi');
console.log('registry ok');
```

- [ ] **Step 2: Chạy fail**

Run: `node tests/tournament/registry.test.js`
Expected: FAIL.

- [ ] **Step 3: Implement**

```js
// lib/tournament/engines/index.js
const roundRobin = require('./roundRobin');
const knockout = require('./knockout');
const simple = require('../match/simple');
const mlp = require('../match/mlp');

const SCHEDULE = { round_robin: roundRobin, knockout };
const MATCH = { simple, mlp };

function getScheduleEngine(format) {
  const e = SCHEDULE[format];
  if (!e) throw new Error(`Unknown schedule_format: ${format}`);
  return e;
}
function getMatchEngine(format) {
  const e = MATCH[format];
  if (!e) throw new Error(`Unknown match_format: ${format}`);
  return e;
}

module.exports = { getScheduleEngine, getMatchEngine };
```

- [ ] **Step 4: Chạy pass**

Run: `node tests/tournament/registry.test.js`
Expected: PASS — `registry ok`.

- [ ] **Step 5: Commit**

```bash
git add lib/tournament/engines/index.js tests/tournament/registry.test.js
git commit -m "feat(tournament): engine registry"
```

---

## Task 11: Orchestrator (mix: round-robin → knockout seeding)

**Files:**
- Create: `lib/tournament/orchestrator.js`
- Test: `tests/tournament/orchestrator.test.js`

- [ ] **Step 1: Viết test fail**

```js
// tests/tournament/orchestrator.test.js
const { seedNextStage, isStageComplete } = require('../../lib/tournament/orchestrator');
const assert = (c, m) => { if (!c) { console.error(`FAIL: ${m}`); process.exit(1); } };

// stage1 round-robin 2 bảng, mỗi bảng top-2 đi tiếp
const stage1 = { schedule_format: 'round_robin', config: { groupCount: 2, advancePerGroup: 2 } };
const entrants = [1, 2, 3, 4, 5, 6, 7, 8].map((id) => ({ id, name: 'E' + id, seed: id }));
// standings giả: bảng A rank 1..4 = 1,4,5,8 ; bảng B = 2,3,6,7
const standings = [
  { entrant_id: 1, group_label: 'A', rank: 1 }, { entrant_id: 4, group_label: 'A', rank: 2 },
  { entrant_id: 5, group_label: 'A', rank: 3 }, { entrant_id: 8, group_label: 'A', rank: 4 },
  { entrant_id: 2, group_label: 'B', rank: 1 }, { entrant_id: 3, group_label: 'B', rank: 2 },
  { entrant_id: 6, group_label: 'B', rank: 3 }, { entrant_id: 7, group_label: 'B', rank: 4 },
];
const seeded = seedNextStage(stage1, standings);
assert(seeded.length === 4, '4 đội đi tiếp');
// cross-bảng: nhất A và nhất B không cùng nửa; seed 1 = nhất A
assert(seeded[0].entrant_id === 1 && seeded[0].seed_in_stage === 1, 'nhất A seed 1');
assert(seeded.map((s) => s.entrant_id).sort().join(',') === '1,2,3,4', 'đúng 4 đội đi tiếp');

assert(isStageComplete([{ status: 'done' }, { status: 'done' }]) === true, 'mọi trận done');
assert(isStageComplete([{ status: 'done' }, { status: 'pending' }]) === false, 'còn trận chưa xong');
console.log('orchestrator ok');
```

- [ ] **Step 2: Chạy fail**

Run: `node tests/tournament/orchestrator.test.js`
Expected: FAIL.

- [ ] **Step 3: Implement**

```js
// lib/tournament/orchestrator.js
const { getScheduleEngine } = require('./engines');

// Stage hoàn tất khi mọi trận đã 'done'.
function isStageComplete(matches) {
  return matches.length > 0 && matches.every((m) => m.status === 'done');
}

// Từ BXH stage hiện tại -> danh sách entrant + seed cho stage kế (gọi advance của engine).
function seedNextStage(stage, standings) {
  const engine = getScheduleEngine(stage.schedule_format);
  return engine.advance(stage, standings);
}

module.exports = { isStageComplete, seedNextStage };
```

- [ ] **Step 4: Chạy pass**

Run: `node tests/tournament/orchestrator.test.js`
Expected: PASS — `orchestrator ok`.

- [ ] **Step 5: Đăng ký script tổng + commit**

Thêm vào `package.json` scripts:
`"test:t-engines": "node tests/tournament/seeding.test.js && node tests/tournament/match-simple.test.js && node tests/tournament/match-mlp.test.js && node tests/tournament/round-robin-schedule.test.js && node tests/tournament/round-robin-groups.test.js && node tests/tournament/round-robin-standings.test.js && node tests/tournament/knockout-schedule.test.js && node tests/tournament/knockout-standings.test.js && node tests/tournament/registry.test.js && node tests/tournament/orchestrator.test.js"`

```bash
git add lib/tournament/orchestrator.js tests/tournament/orchestrator.test.js package.json
git commit -m "feat(tournament): multi-stage orchestrator (mix seeding)"
```

---

## QA Gate (incremental — `tournament-qa`)

Sau Task 11, chạy toàn bộ engine test và đối chiếu shape với spec §4 trước khi sang Plan 2 (API):

- [ ] Chạy `npm run test:t-engines` → tất cả `... ok`.
- [ ] Đối chiếu shape: output `generateSchedule` (slot/parent_slot) khớp cột `tournament_matches` (bracket_slot, parent_match_id) — Plan 2 sẽ map slot→id khi insert.
- [ ] `resolveMatch` trả `points_a/points_b/games_a/games_b` đúng cái `computeStandings` tiêu thụ (đặt vào match đã chốt).
- [ ] Ghi `_workspace/02_engine_api.md`: liệt kê chữ ký hàm + shape thực tế cho api-dev.

---

## Self-Review (đã rà)

- **Spec coverage:** §2 mô hình lõi → Task 5–11; §3 schema → Task 1; §4 engine (2 trục + orchestrator) → Task 3–11; §8 testing → mọi task TDD. (UI/API ở Plan 2–3.)
- **Type consistency:** entrant `{id,name,seed}`, match sinh `{round,group_label,bracket_slot,parent_slot,slot,entrant_a_id,entrant_b_id,order}`, match đã chốt `{...,winner_entrant_id,points_a,points_b,games_a,games_b,status}`, standings `{entrant_id,...,rank}`, `resolveMatch -> {winner_entrant_id,games_a,games_b,points_a,points_b,complete}`, `advance -> {entrant_id,seed_in_stage,...}` — nhất quán giữa các task.
- **Placeholder:** không còn TBD; mọi step có code thật.

## Plan kế tiếp (chưa viết)
- **Plan 2 — API** `tournament-v2`: CRUD giải/stage/entrant, generate (map slot→match id + parent), games (gọi match engine, set winner + status), standings (gọi computeStandings), advance (tạo stage_entrants kế), public/[slug], realtime.
- **Plan 3 — UI**: wizard 4 bước, console tab, trang công khai + QR + realtime, mobile-first.
