# T1.C Handoff

## Scope
- Task ID: `T1.C`.
- Route ownership checked before editing: `app/api/tournament-v2/athletes/route.js`, `app/api/tournament-v2/pairings/route.js`, `app/api/tournament-v2/entrants/route.js`.
- Actual route changes: `athletes/route.js` and `pairings/route.js`; `entrants/route.js` was inspected and left unchanged.
- Added private domain helper: `lib/tournament/participantContract.js`.
- Added tests: `tests/unified-setup-v2/participants/participant-contract.test.js`, `tests/unified-setup-v2/participants/run-all.js`.
- Did not modify T1.B aggregate/setup/finalize or shared entry points/package.json/frozen files.

## Contract behavior
- Roster athlete lookup is tenant-scoped by `group_id`.
- Pair validation rejects duplicate membership and invalid pair sizes.
- Pair application preserves stable pair IDs and locked state on replay.
- Helper exposes explicit `ATHLETE_ID_MISSING` and `MEMBER_OUTSIDE_GROUP` errors.
- Preview behavior remains read-only; no preview route was changed.

## Tests
- Red first: `node tests/unified-setup-v2/participants/participant-contract.test.js` failed with `Cannot find module .../participantContract`.
- Green after implementation: `node tests/unified-setup-v2/participants/run-all.js` passed: `participant contract red/green test ok`.
- `git diff --check` reports one pre-existing trailing whitespace in `docs/superpowers/plans/2026-09-19-unified-internal-tournament-setup-parallel.md`; it was not changed.

## Risks / follow-up
- The existing pair confirmation RPC remains the persistence boundary; full live CAS/idempotency verification requires the configured Supabase environment.
- `entrants/route.js` still contains legacy member resolution and should be reconciled only if lead assigns that route explicitly.
- No contract change or ADR was made.
