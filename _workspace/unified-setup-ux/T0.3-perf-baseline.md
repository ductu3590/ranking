# T0.3 — Baseline hiệu năng luồng tạo giải hiện tại

Ngày ghi nhận: 2026-09-20  
Task: `T0.3`  
Baseline commit đo: `d1355bf4b7785b65b14ac10a64915554e50657ea`  
Phạm vi tenant: provisioned test groups `44` và `45`, đã cleanup an toàn; không dùng CLB thật.

## Kết luận ngắn

Gate T0.3-live **PASS**: script `scripts/qa/perf-baseline-unified-setup.js` đo được 3/4 luồng chính trên UI cũ. `Lưu nháp` giữ `BLOCKED` hợp lệ vì wizard cũ không có hành động lưu nháp riêng; draft ghi vào browserStorage đồng bộ và không phát request.

Artifact đầy đủ cho Wave 4: `_workspace/unified-setup-ux/perf-evidence/2026-09-20T02-03-29-088Z/`.

## Cấu hình máy đã ghi nhận

| Thuộc tính | Giá trị |
|---|---|
| OS | Windows |
| Browser | Google Chrome executable `C:\Program Files\Google\Chrome\Application\chrome.exe` |
| Chromium version | `153.0.8010.52` |
| Node | v25.3.0 |
| npm | 11.6.2 |
| Viewport | Desktop `1440x900` |
| Server | `npm run dev -- -p 3100`, `http://127.0.0.1:3100`, warmup HTTP `200` |
| Số VĐV | `14` |
| Giờ đo | `2026-09-20T02:06:11.353Z` |
| group_id đo | `44` |

## Bảng baseline — đo thật

Commit đo: `d1355bf4b7785b65b14ac10a64915554e50657ea` · Thời điểm: 2026-09-20T02:06:11.353Z · Chromium 153.0.8010.52
Viewport 1440x900 · 5 lần đo sau 1 warm-up · 14 VĐV · group_id `44`

| Luồng | Request count | Thời gian (ms) | Route chính | Payload | Ghi chú |
|---|---:|---|---|---|---|
| Mở nháp | 5 | med 1634 / p95 1701 / min 1600 / max 1701 | GET /giai-dau/v2 → wizard render | 2506 B | n=5 |
| Lưu nháp | `BLOCKED` | `BLOCKED` | — | `BLOCKED` | wizard cũ KHÔNG có hành động lưu nháp riêng; bản nháp ghi vào browserStorage đồng bộ, không phát request |
| Preview lịch | 1 | med 1099 / p95 1245 / min 1083 / max 1245 | POST /api/tournament-v2/preview-schedule | 1027 B | n=5 |
| Chốt tạo giải | 35 | med 17268 / p95 17394 / min 17152 / max 17394 | chuỗi checkpoint qua /api/tournament-v2/* | 1763820 B | n=5 |

## Tải trùng

| Hạng mục | Tổng request | Bị lặp | Bytes |
|---|---:|---|---:|
| Roster (1 lần mở wizard) | 1 | không | 13 |
| Sân (1 lần mở wizard) | 0 | không | 0 |
| Danh sách giải (1 lần mở dashboard) | 2 | GET /api/tournament-v2/tournaments ×2 | 27372 |
| Toàn bộ API khi chốt | 31 | POST /api/tournament-v2/entries ×14<br>GET /api/tournament-v2/tournaments ×3<br>GET /api/groups/session ×2<br>GET /api/tournament-v2/divisions?tournamentId=162 ×2<br>GET /api/tournament-v2/stages?tournamentId=162 ×2<br>GET /api/tournament-v2/assignments?tournamentId=162 ×4 | 55660 |

## Phản hồi giao diện sau thêm/bỏ người

| Thao tác | Request count | Click → UI settled (ms) |
|---|---:|---:|
| Thêm một người | 0 | 595 |
| Bỏ một người | 0 | 586 |

## Evidence live attempt

- HEAD lúc đo: `d1355bf4b7785b65b14ac10a64915554e50657ea`.
- `TournamentSetupWorkspace` grep trong `app/`: chỉ có file setup workspace, UI cũ vẫn live theo guard của script.
- Env file provision tạm: `C:\Users\ductu\AppData\Local\Temp\pickhub-t03-live-v2.env` (đã xoá sau cleanup; không ghi secrets).
- Provision command: `node scripts/qa/provision-browser-harness.js "$env:TEMP\pickhub-t03-live-v2.env"` với `PICKHUB_QA_BASE_URL=http://127.0.0.1:3100`.
- Provision output: tenant A `group_id=44`, tenant B `group_id=45`.
- Measurement command: `node scripts/qa/perf-baseline-unified-setup.js` với `CHROME_EXECUTABLE=C:\Program Files\Google\Chrome\Application\chrome.exe`.
- Artifact dir: `_workspace/unified-setup-ux/perf-evidence/2026-09-20T02-03-29-088Z/` gồm `perf-baseline.json` và `perf-baseline.md`.
- Selector fix trong script: preview được kích hoạt bằng đổi cấu hình UI cũ; mỗi run clear `localStorage/sessionStorage`; seed chốt tạo giải dùng luồng cá nhân để finalize đo được ổn định.

## Cleanup evidence

Command:

`Get-Content .env.local | ForEach-Object { if ($_ -match '^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=\s*["'']?([^"''\r\n]*)["'']?\s*$') { [Environment]::SetEnvironmentVariable($matches[1], $matches[2], 'Process') } }; node scripts/qa/cleanup-browser-harness.js 44 45`

Output summary:

- Group `44`: `clean: true`; before cleanup included `tournaments: 21`, `tournament_divisions: 21`, `tournament_stages: 21`, `tournament_athletes: 210`, `tournament_division_roster_members: 210`, `tournament_entries: 84`, `tournament_entry_members: 84`; after cleanup all scoped counts `0`.
- Group `45`: `clean: true`; before cleanup included `group_sessions: 4`, `groups: 1`; after cleanup all scoped counts `0`.
- Cleanup mode: `delete`.
- Env temp file `C:\Users\ductu\AppData\Local\Temp\pickhub-t03-live-v2.env` không còn tồn tại sau cleanup.

## Cách tái lập cho Wave 4

1. Khởi động server repo trên port `3100`: `npm run dev -- -p 3100`.
2. Provision hai CLB test ngoài cây source bằng `node scripts/qa/provision-browser-harness.js <env-file>`; chỉ dùng tenant A để đo. Ghi `group_id`, không ghi cookie/password vào báo cáo hoặc git.
3. Load env bash-style từ file provision vào PowerShell process rồi đặt `CHROME_EXECUTABLE` nếu Playwright bundled Chromium chưa có.
4. Chạy `node scripts/qa/perf-baseline-unified-setup.js`; script tự warm-up 1 lần, đo 5 lần, ghi artifact vào `_workspace/unified-setup-ux/perf-evidence/<timestamp>/`.
5. Cleanup bằng `node scripts/qa/cleanup-browser-harness.js <groupIdA> <groupIdB>` sau khi load `.env.local`; xác nhận mọi scoped count về `0`.

## Rủi ro / giới hạn

- Đây là baseline desktop local dev server, không phải production hosting benchmark.
- `Lưu nháp` không có số là đúng với UI cũ; không dùng số giả.
- Chốt tạo giải qua legacy checkpoint cá nhân tạo nhiều entry request lặp; Wave 4 cần so sánh cùng điều kiện hoặc ghi rõ baseline mới nếu flow thay đổi.
